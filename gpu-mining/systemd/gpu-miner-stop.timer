[Unit]
Description=Stop GPU miner at off-peak window close (edit OnCalendar to your tariff)

[Timer]
# Default: 07:00 local time, every day — pair with gpu-miner-start.timer.
OnCalendar=*-*-* 07:00:00
Persistent=true
Unit=gpu-miner-stop.service

[Install]
WantedBy=timers.target
